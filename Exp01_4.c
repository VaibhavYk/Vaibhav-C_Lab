#include <stdio.h>
int main()
{
    int a, b, s;
    printf("Enter the first number:");
    scanf("%d",&a);
    printf("Enter the second number:");
    scanf("%d",&b);

    s=a+b;

    printf("Sum of numbers:%d", s);
    return 0;
}